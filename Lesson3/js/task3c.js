'use strict';

function squareof(n) {
    return n * n
}



console.log(squareof('125'));

function binarySearch(target) {
    let min = 1;
    let max = 100;

    while (min <= max) {
        mid = (min + max) / 2;
        if ()
    }



    return
}

let num = prompt("Загадате целое число от 1 до 100 \n"+
            'Загаданное число больше 50?\n'+
            '')